#!/bin/sh
command -v git-lfs >/dev/null 2>&1 || { printf >&2 "\n%s\n\n" "Note: Git LFS is not installed — skipping LFS post-checkout. Install from https://git-lfs.com if you need LFS support."; exit 0; }
git lfs post-checkout "$@"
